int main() {
    int length, breadth;
    // Ask user to enter length and breadth
    printf("Enter length of rectangle: ");
    scanf("%d", &length);

    printf("Enter breadth of rectangle: ");
    scanf("%d", &breadth);

    // Calculate area
    int area = length * breadth;

    // Display Result
    printf("Area of sexy Rectangle is = %d\n", area);
    return 0;  // tells the OS the program ended successfully
}

// i have used chatgpt but i have understood whatever i have written. I am learning C before python. Do hard things first.
